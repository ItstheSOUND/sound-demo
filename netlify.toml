[build]
  publish = "."
  command = ""

# Static, self-contained prototype. The player is index.html (no email gate).
# Keep old /prototype.html links working:
[[redirects]]
  from = "/prototype.html"
  to = "/"
  status = 301
